%h1 Listing launches
%table
  %tr
    %th Name
    %th Settings
    %th State
  - @launches.each_with_index do |launch, idx|
    %tr
      %td{:style => "border-bottom: solid 1px grey;"}= h launch.name
      %td= h ActiveSupport::JSON.decode(launch.settings)['file_prefix'] rescue "ERROR"
      %td= h launch.state
      %td= link_to 'Show', launch
      %td= link_to 'Edit', edit_launch_path(launch)
      %td= link_to 'Destroy', launch, :confirm => 'Are you sure?', :method => :delete
%br/
= link_to 'New launch', new_launch_path
