.dashboard
  %span 
    %h5 
      create new launch:
      - @subapps.each_with_index do |subapp, i|
        = "," if i > 0
        =link_to subapp.name, :controller => :launches, :action => :new
    
      
  =remote_module "mod1"


